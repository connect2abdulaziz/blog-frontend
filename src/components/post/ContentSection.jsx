// components/ContentSection.jsx
import React from 'react';

const ContentSection = ({ content }) => {
  return (
      <p className="w-[900px] text-[15px] leading-[22px] text-gray-600">{content}</p>
  );
};

export default ContentSection;
